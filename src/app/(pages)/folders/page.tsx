import { getUsers } from "@/api/controllers/user/getUsers";
import React, { FC } from "react";

type PageProps = {};

const Folders: FC<PageProps> = async (props) => {
  // const user = await getUsers();
  // console.log(user);
  return <div>пуста</div>;
};

export default Folders;
