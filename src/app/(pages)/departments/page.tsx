import Title from "@/components/title/title";
import React, { FC } from "react";

type PageProps = {};

const Departments: FC<PageProps> = (props) => {
  return (
    <div>
      <Title title="Віділи" />
    </div>
  );
};

export default Departments;
