import Title from "@/components/title/title";
import React, { FC } from "react";

type PageProps = {};

const Structure: FC<PageProps> = (props) => {
  return (
    <div>
      <Title title="Структура" />
    </div>
  );
};

export default Structure;
